<div class="container">
  <div class="show-img-box mt-5">
  </div>


  <div class="card" style="width: 18rem;">
    <img src="..." class="card-img-top" alt="...">
    <div>
      <img class="avatar-large" alt="avatar-large" src="" />
    </div>
    <div class="card-body">
      <h3 class="card-title"><%= @story.title %></h3>
      <p class="card-text"><%= @story.description %></p>
      <h4>Who is it we want to reach?</h4>
      <!-- criteria -->
      <h4>About us</h4>
      <p><%= @story.business.description %></p>
    </div>
  </div>
  <a href="#" class="btn btn-custom btn-custom-dark">Start a conversation</a>
  <%= link_to 'Edit this story', edit_business_story_path(@business, @story), class: 'btn btn-custom btn-custom-dark-outline' %>

</div>
