<h1>This is the stories index</h1>

<% @stories.each do |story| %>
  <%= link_to story.title, business_story_path(@business, story)  %>
  <% story.photos.each do |photo| %>
  <%= cl_image_tag photo.key %>
  <% end %>
<% end %>
